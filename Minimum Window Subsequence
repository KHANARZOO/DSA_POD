class Solution {
    public String minWindow(String s1, String s2) {
        int start = -1;
        int maxLen = Integer.MAX_VALUE;
        int l1 = s1.length();
        int l2 = s2.length();
        // boolean vis[] = new boolean[l1];
        // Arrays.fill(vis, false);
        // while(true){
        //     int i = 0;
        //     int j = 0;
        //     int innerStart = -1;
        //     while(i < l1 && j < l2){
        //         if(s1.charAt(i) == s2.charAt(j) && !vis[i]){
        //             if(innerStart == -1){
        //                 innerStart = i;
        //                 vis[i] = true;
        //             }
        //             j++;
        //         }
        //         if(j == l2){
        //             int len = i - innerStart + 1;
        //             if(maxLen > len){
        //                 maxLen = len;
        //                 start = innerStart;
                        
        //             }
        //         }
        //         i++;
        //     }
        //     if(innerStart == -1)break;
        // }
        // if(start == -1) return "";
        // else return s1.substring(start, start + maxLen);
        
        
        
        
        for(int i = 0; i < l1; i++){
            int j = i;
            int k = 0;
            while(j < l1 && k < l2){
                if(s1.charAt(j) == s2.charAt(k)){
                    k++;
                }
                if(k == l2){
                    int len = j - i + 1;
                    if(maxLen > len){
                        maxLen = len;
                        start = i;
                    }
                }
                j++;
            }
            if(k == l2){
                int len = j - i + 1;
                if(maxLen > len){
                    maxLen = len;
                    start = i;
                }
            }
        }
        if(start == -1) return "";
        else return s1.substring(start, start + maxLen);

    }
}
